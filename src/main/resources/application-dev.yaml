spring:
  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    uz.myrafeeq.api: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

myrafeeq:
  cors:
    allowed-origins:
      - "http://localhost:*"
